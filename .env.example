# 环境变量模板 —— 提交到 GitHub 的只有这份
# 使用方法：复制为 .env.development（本地）和 .env.production（服务器）后填写

VITE_APP_NAME=my-site
VITE_API_BASE=/api

MONGODB_URI=mongodb://127.0.0.1:27017/my-site
PORT=3000
JWT_SECRET=change-me
